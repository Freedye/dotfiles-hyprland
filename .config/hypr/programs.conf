# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = pgrep wofi >/dev/null 2>&1 && killall wofi || wofi --show drun
$hyprlock = hyprlock
$screenshoot = grim -g "$(slurp)" -t png - | tee -a "/home/freedye/Immagini/Screenshot $(date +%c)" | wl-copy -t image/png